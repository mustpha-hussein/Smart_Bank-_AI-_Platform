[project]
name = "banking-ml-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mustafa Hussein", email = "mustapha@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
banking-ml-platform = "banking_ml_platform:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
